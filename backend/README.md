# backend

Make sure you are in the `backend` directory before doing any of these. To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

To test:

```bash
bun test
```